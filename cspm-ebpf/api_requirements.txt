# Sentinel-Core API Requirements
# FastAPI application dependencies

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Data Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP Client (for external API calls if needed)
httpx>=0.25.0

# CORS Support (included with FastAPI)
# starlette  # Comes with FastAPI

# Testing (optional, for running tests)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0

# Development (optional, for better debugging)
python-dotenv>=1.0.0

# Production Deployment (optional)
gunicorn>=21.0.0  # For production WSGI server
